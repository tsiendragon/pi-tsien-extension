/**
 * Temporary compatibility shim — `lib/` now lives in the `pi-tsien-shared` package.
 * Kept only for the not-yet-migrated consumer (live-session.ts); delete it once that moves.
 */
export * from "pi-tsien-shared/src/lib/live-observer.ts";
